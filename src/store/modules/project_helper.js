export default class Project {
    constructor(
        name,
        budget,
        creator,
        tasks
    ) {
        this.name = name
        this.budget = budget
        this.creator = creator
        this.tasks = tasks
    }
}
